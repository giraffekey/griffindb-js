version: "3.9"
services:
  app:
    build: .
    ports:
      - 8765:8765
      - 8766:8766
      - 8767:8767
  nginx:
    image: nginx
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
